import * as React from "react"
import { Root as VisuallyHiddenPrimitive } from "@radix-ui/react-visually-hidden"

// Simple re-export to match shadcn/ui convention
export const VisuallyHidden = VisuallyHiddenPrimitive

export default VisuallyHidden
